import type { EventDefinition } from '../core/events';

// Populated from verified public references (Bulbapedia/Serebii) - see each entry's `source`.
export const EVENTS_GEN4: EventDefinition[] = [];
